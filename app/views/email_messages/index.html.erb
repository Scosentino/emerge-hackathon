<p id="notice"><%= notice %></p>

<h1>Email Messages</h1>

<table>
  <thead>
    <tr>
      <th>recipient email</th>
      <th>Recipient full name</th>
      <th>Recipient preferred language</th>
      <th>Sender full name</th>
      <th>Sender email</th>
      <th>Transcription</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @email_messages.each do |email_message| %>
      <tr>
        <td><%= email_message.recipient_email %></td>
        <td><%= email_message.recipient_full_name %></td>
        <td><%= email_message.recipient_preferred_language %></td>
        <td><%= email_message.sender_full_name %></td>
        <td><%= email_message.sender_email %></td>
        <td><%= email_message.transcription %></td>
        <td><%= link_to 'Show', email_message %></td>
        <td><%= link_to 'Edit', edit_email_message_path(email_message) %></td>
        <td><%= link_to 'Destroy', email_message, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Email Message', new_email_message_path %>
